from setuptools import setup, find_packages

setup(
    name="use_root",
    version='1.0.0',
    url='https://github.com/ecchochan/use_root.git',
    author='Echo Chan',
    author_email='ecchochan@gmail.com',
    description='Allow script to import from project root',
    packages=find_packages(),    
)